diff --git a/meson.build b/meson.build
index 58b21e9..eb87c95 100644
--- a/meson.build
+++ b/meson.build
@@ -92,7 +92,7 @@ if not freetype2_dep.found()
                               method: 'cmake',
                               fallback: ['freetype2', 'freetype_dep'])
 endif
-pixbuf_dep     = dependency('gdk-pixbuf-2.0',
+pixbuf_dep     = dependency('gdk-pixbuf-2.0-headers',
                             version: gdk_pixbuf_required,
                             required: get_option('pixbuf'),
                             fallback: ['gdk-pixbuf', 'gdkpixbuf_dep'],
diff --git a/meson_options.txt b/meson_options.txt
index 5c89adf..d943092 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -8,14 +8,14 @@ option('introspection',
 
 option('pixbuf',
        type: 'feature',
-       value: 'auto',
+       value: 'enabled',
        yield: true,
        description: 'Build with GDK-Pixbuf'
        )
 
 option('rsvg-convert',
        type: 'feature',
-       value: 'enabled',
+       value: 'disabled',
        yield: true,
        description: 'Build the rsvg-convert util to convert SVG files to other image formats'
        )
@@ -29,19 +29,19 @@ option('pixbuf-loader',
 
 option('docs',
        type: 'feature',
-       value: 'auto',
+       value: 'disabled',
        yield: true,
        description: 'Build API reference and rsvg-convert documentation (requires gi-docgen and rst2man)')
 
 option('vala',
        type: 'feature',
-       value: 'auto',
+       value: 'disabled',
        yield: true,
        description: 'Build Vala bindings (requires the vala compiler)')
 
 option('tests',
        type: 'boolean',
-       value: true,
+       value: false,
        yield: true,
        description: 'Build the test suite')
 
diff --git a/rsvg/meson.build b/rsvg/meson.build
index dc29b88..69117c5 100644
--- a/rsvg/meson.build
+++ b/rsvg/meson.build
@@ -276,7 +276,7 @@ endif
 # This is the main dependency to use for tests; it means "link to the library we just built"
 librsvg_dep = declare_dependency(
   sources: includes,
-  include_directories: [includeinc],
+  include_directories: includeinc,
   dependencies: library_dependencies,
   compile_args: librsvg_dep_args,
   link_with: librsvg_lib,
